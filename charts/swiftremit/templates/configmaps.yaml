{{- range list "api" "backend" "frontend" }}
{{- $svc := index $.Values . }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "daddy-config.fullname" $ }}-{{ . }}
  labels:
    {{- include "daddy-config.labels" $ | nindent 4 }}
    app.kubernetes.io/component: {{ . }}
data:
  {{- range $k, $v := $svc.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
{{- end }}
